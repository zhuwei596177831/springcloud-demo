server:
  port: 8762
spring:
  application:
    name: eureka-server2
  security:
    user:
      name: zhuweiwei1
      password: zhuweiwei1
      roles: SERVICE_NODE
eureka:
  instance:
    hostname: server2
    instance-id: ${eureka.instance.hostname}:${server.port}:@springcloud.demo.project.version@
  client:
    healthcheck:
      enabled: false
    fetch-registry: true
    register-with-eureka: false
    service-url:
      defaultZone: http://zhuweiwei:zhuweiwei@server1:8761/eureka/